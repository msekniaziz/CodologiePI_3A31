<!DOCTYPE html>

<title>Blog</title>

{% block body %}
    <h1>Blog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ blog.category }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ blog.titre }}</td>
            </tr>
            <tr>
                <th>Contenu_blog</th>
                <td>{{ blog.contenuBlog }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ blog.status }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ blog.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_blog_index') }}">back to list</a>

    <a href="{{ path('app_back_blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('back_blog/_delete_form.html.twig') }}
{% endblock %}
