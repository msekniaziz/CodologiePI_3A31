<form method="post" action="{{ path('app_type_dispo_delete', {'id': type_dispo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_dispo.id) }}">
    <button class="btn" style="color: red;" ><i class="fa fa-trash"></i>Delete</button>                
    {# <button class="btn" style="color: green;" ><i class="fa fa-edit"></i>Delete</button>                 #}
</form>
