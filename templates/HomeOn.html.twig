{% extends 'front.html.twig' %}
{% block home %}
    <li class="nav-item @@home">
        <a class="nav-link" href="{{ path('homeOn') }}">Home</a>
    </li>
{% endblock %}

{% block logout %}

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
        <header>
            <ul class="navbar-nav ml-auto mt-10">
                <li class="nav-item">
                    <a class="fas fa-power-off" href="{{ path('app_logout') }}"></a>
                </li>
            </ul>
        </header>
{% endblock %}

    {% block ngo %}
        <li><a class="dropdown-item @@category" href="{{ path('app_association_indexHomeOn') }}">NGOs</a></li>  
  {% endblock %}

{% block ajouterannonce %}

{% endblock %}
{% block edit_account %}
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>User Information</title>
        <!-- Your custom CSS styles can go here -->
        <style>
            /* Add any custom styles here */
            .navbar-nav {
                list-style: none;
                padding: 0;
                margin: 0;
            }

            .nav-item {
                position: relative;
            }

            .dropdown-content {
                display: none;
                position: absolute;
                top: 100%;
                left: 0;
                background-color: #f9f9f9;
                min-width: 160px;
                box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
                z-index: 1;
            }

            .nav-item:hover .dropdown-content {
                display: block;
            }

            {# style lel affichage linda #}
            .association-container {
    display: flex;
    flex-wrap: wrap;
}

.association {
    width: 300px; /* Adjust width as needed */
    margin: 10px;
    padding: 10px;
    border: 1px solid #ccc;
    background-color: #f9f9f9;
    display: inline-block; /* Display associations side by side */
    vertical-align: top; /* Align associations at the top of the container */
}
 .error-message {
            color: red; /* Couleur rouge pour les messages d'erreur */
        }

        </style>
    </head>
    <header>
        <ul class="navbar-nav ml-auto mt-10">
            <li class="nav-item">
                <a class="nav-link login-button" href="{{ path('user_modify', {'id': app.user.getId()}) }}">{{ app.user.getNom() }} {{ app.user.getPrenom() }}</a>
                <div class="dropdown-content">
                    <a href="#">User Information</a>
                    <br>
                    <a href="#">Products</a>
                    
                    <br>
                    <a href="#">Blogs</a>
                    <br>
                    <a href="#">My Products</a>
                    <br>
                    <a href="#">My Products</a>
                    <br>
                    <a href="#">My Products</a>

                </div>
            </li>
        </ul>
    </header>
{% endblock %}
{% block body %}
    {% if app.user %}
        <div class="mb-3">
            Welcome {{ app.user.getNom() }} {{ app.user.getPrenom() }}
        </div>
    {% endif %}
{% endblock %}
