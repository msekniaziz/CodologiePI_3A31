
{% extends 'back.html.twig' %}


{% block donbien %}
<title>DonBienMateriel</title>
    <h3>Material goods donation</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ don_bien_materiel.id }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td><img class="card-img-top img-fluid" src="{{ asset('uploads/images/' ~ don_bien_materiel.photoDon) }}" alt="Card image cap" style="height: 100px;width: 100px";></td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ don_bien_materiel.descriptionDon }}</td>
            </tr>
            <tr>
                <th>Statuts</th>
                <td>{{ don_bien_materiel.statutDon ? 'Checked' : 'Not checked' }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ don_bien_materiel.dateDonBienMateriel ? don_bien_materiel.dateDonBienMateriel|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div class="button-container">

<button class="btn" style="color: blue;" onclick="window.location.href='{{ path('app_don_bien_materiel_indexback') }}'">
        <i class="fa fa-list"></i> Back to list
    </button>
                    <button class="btn" style="color: green;" onclick="window.location.href='{{ path('app_don_bien_materiel_edit', {'id': don_bien_materiel.id})}}'">
        <i class="fa fa-edit"></i> Edit
    </button> 
    {# <a href="{{ path('app_don_bien_materiel_indexback') }}" class="button">back to list</a>

    <a href="{{ path('app_don_bien_materiel_edit', {'id': don_bien_materiel.id}) }}">edit</a> #}

    {# {{ include('don_bien_materiel/_delete_form.html.twig') }} #}
</div>
{% endblock %}

