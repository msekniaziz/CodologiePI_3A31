<!DOCTYPE html>

<title>ProduitTrocWith</title>

{% block body %}
    <h1>ProduitTrocWith</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_troc_with.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit_troc_with.nom }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ produit_troc_with.category }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit_troc_with.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit_troc_with.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_troc_with_index') }}">back to list</a>

    <a href="{{ path('app_produit_troc_with_edit', {'id': produit_troc_with.id}) }}">edit</a>

    {{ include('produit_troc_with/_delete_form.html.twig') }}
{% endblock %}
