<form method="post" action="{{ path('app_don_argent_delete', {'id': don_argent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ don_argent.id) }}">
    <button class="btn">Delete</button>
</form>
