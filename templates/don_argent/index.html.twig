<!DOCTYPE html>

<title>DonArgent index</title>

{% block body %}
    <h1>DonArgent index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Montant</th>
                <th>DateDonArgent</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for don_argent in don_argents %}
            <tr>
                <td>{{ don_argent.id }}</td>
                <td>{{ don_argent.montant }}</td>
                <td>{{ don_argent.dateDonArgent ? don_argent.dateDonArgent|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_don_argent_show', {'id': don_argent.id}) }}">show</a>
                    <a href="{{ path('app_don_argent_edit', {'id': don_argent.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_don_argent_new') }}">Create new</a>
{% endblock %}
