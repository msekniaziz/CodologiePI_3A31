
{% extends 'back.html.twig' %}


{% block dona %}
<title>DonArgent</title>
    <h3>Money donation</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ don_argent.id }}</td>
            </tr>
           
            <tr>
                <th>Amount</th>
                <td>{{ don_argent.montant }}</td>
            </tr>
            
            <tr>
                <th>Date</th>
                <td>{{ don_argent.dateDonArgent ? don_argent.dateDonArgent|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div class="button-container">

<button class="btn" style="color: blue;" onclick="window.location.href='{{ path('app_don_argent_indexback') }}'">
        <i class="fa fa-list"></i> Back to list
    {# </button>
                    <button class="btn" style="color: green;" onclick="window.location.href='{{ path('app_don_argent_edit', {'id': don_argent.id})}}'">
        <i class="fa fa-edit"></i> Edit
    </button>  #}
    {# <a href="{{ path('app_don_argent_indexback') }}" class="button">back to list</a>

    <a href="{{ path('app_don_argent_edit', {'id': don_argent.id}) }}">edit</a> #}

    {# {{ include('don_argent/_delete_form.html.twig') }} #}
</div>
{% endblock %}

